using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Microsoft.Xna.Framework;

namespace MapEditor
{
    public static class Extensions
    {
        //public static bool Contains(this Rectangle rectangle, Vector2 cursor)
        //{
        //    bool XWithin = cursor.X > rectangle.X && (rectangle.X + rectangle.Width) > cursor.X;
        //    bool YWithin = cursor.X > rectangle.Y && (rectangle.Y + rectangle.Height) > cursor.Y;

        //    return XWithin && YWithin;
        //}
    }
}
